/* global define */

(function () {

    'use strict';

    var dependencies = ['angular'];

    define(dependencies, function (angular) {

        return angular.module('app.dashboard', []);

    });

})();
